/**
 * sheffield Scrapers - 23 scrapers
 */

const scrapeCodeNightclub = require('./code_nightclub');
const scrapeCorporation = require('./corporation');
const scrapeCrucibleTheatre = require('./crucible_theatre');
const scrapeDocFestSheffield = require('./doc_fest_sheffield');
const scrapeEndcliffeParkEvents = require('./endcliffe_park_events');
const scrapeFoundry = require('./foundry');
const scrapeKelhamIslandFest = require('./kelham_island_fest');
const scrapeLeadmill = require('./leadmill');
const scrapeO2academy = require('./o2academy');
const scrapeOffTheShelfFest = require('./off_the_shelf_fest');
const scrapePeddlerMarketSheff = require('./peddler_market_sheff');
const scrapeSheffSummerStreets = require('./sheff_summer_streets');
const scrapeSheffieldBeerWeek = require('./sheffield_beer_week');
const scrapeSheffieldFoodFest = require('./sheffield_food_fest');
const scrapeSheffieldPride = require('./sheffield_pride');
const scrapeSidneyAndMatilda = require('./sidney_and_matilda');
const scrapeTheHarley = require('./the_harley');
const scrapeTramlinesFestival = require('./tramlines_festival');
const scrapeUtilitaArena = require('./utilita_arena');
const scrapeWinterGardenEvents = require('./winter_garden_events');
const scrapeYellarch = require('./yellarch');
const scrapeTankSheffield = require('./tank_sheffield');
const scrapeHopeWorksSheffield = require('./hope_works_sheffield');
const scrapePlug = require('./plug_sheffield');
const scrapeDryDog = require('./drydog_sheffield');
const scrapeDQ = require('./dq_sheffield');
const scrapeRevolution = require('./revolution_sheffield');
const scrapeBarOne = require('./bar_one_sheffield');

module.exports = {
  scrapeCodeNightclub,
  scrapeCorporation,
  scrapeCrucibleTheatre,
  scrapeDocFestSheffield,
  scrapeEndcliffeParkEvents,
  scrapeFoundry,
  scrapeKelhamIslandFest,
  scrapeLeadmill,
  scrapeO2academy,
  scrapeOffTheShelfFest,
  scrapePeddlerMarketSheff,
  scrapeSheffSummerStreets,
  scrapeSheffieldBeerWeek,
  scrapeSheffieldFoodFest,
  scrapeSheffieldPride,
  scrapeSidneyAndMatilda,
  scrapeTheHarley,
  scrapeTramlinesFestival,
  scrapeUtilitaArena,
  scrapeWinterGardenEvents,
  scrapeYellarch,
  scrapeTankSheffield,
  scrapeHopeWorksSheffield,
  scrapePlug,
  scrapeDryDog,
  scrapeDQ,
  scrapeRevolution,
  scrapeBarOne
};
