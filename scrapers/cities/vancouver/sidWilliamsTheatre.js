/**
 * Sid Williams Theatre Scraper - TEMPORARILY DISABLED
 * Website structure changed causing duplicates
 */

const SidWilliamsTheatreEvents = {
  async scrape(city) {
    console.log('⚠️  Sid Williams Theatre scraper disabled - needs fixing (was causing 642 duplicates)');
    return [];
  }
};

module.exports = SidWilliamsTheatreEvents.scrape;
