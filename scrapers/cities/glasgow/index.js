/**
 * Glasgow Scrapers - Cleaned (1 per venue)
 */

const scrapeAbc = require('./abc');
const scrapeArtSchool = require('./art_school');
const scrapeAyeWriteBookFest = require('./aye_write_book_fest');
const scrapeBarrowland = require('./barrowland');
const scrapeBroadcast = require('./broadcast');
const scrapeCathouse = require('./cathouse');
const scrapeCelticConnections = require('./celtic_connections');
const scrapeCitizen = require('./citizen');
const scrapeClassic = require('./classic');
const scrapeGarage = require('./garage');
const scrapeGlasgowComedyFest = require('./glasgow_comedy_fest');
const scrapeGlasgowDoorsOpen = require('./glasgow_doors_open');
const scrapeGlasgowFilmFest = require('./glasgow_film_fest');
const scrapeGlasgowGreenEvents = require('./glasgow_green_events');
const scrapeGlasgowInternational = require('./glasgow_international');
const scrapeGlasgowJazzFest = require('./glasgow_jazz_fest');
const scrapeGlasgowMela = require('./glasgow_mela');
const scrapeGlasgowPride = require('./glasgow_pride');
const scrapeHampdenParkConcerts = require('./hampden_park_concerts');
const scrapeHydro = require('./hydro');
const scrapeKingTutsWah = require('./king_tuts_wah');
const scrapeMerchantCityFest = require('./merchant_city_fest');
const scrapeMono = require('./mono');
const scrapeNice = require('./nice');
const scrapeO2academy = require('./o2academy');
const scrapeOran = require('./oran');
const scrapePipingLiveFest = require('./piping_live_fest');
const scrapePoetry = require('./poetry');
const scrapeSec = require('./sec');
const scrapeStereo = require('./stereo');
const scrapeSubClub = require('./sub_club');
const scrapeSwg3 = require('./swg3');
const scrapeTrnsmtFestival = require('./trnsmt_festival');
const scrapeWestEndFestival = require('./west_end_festival');
const scrapeWorldPipeBandChamps = require('./world_pipe_band_champs');
const scrapeBambooGlasgow = require('./bamboo_glasgow');
const scrapeSWG3V2 = require('./swg3_v2');

module.exports = {
  scrapeAbc,
  scrapeArtSchool,
  scrapeAyeWriteBookFest,
  scrapeBarrowland,
  scrapeBroadcast,
  scrapeCathouse,
  scrapeCelticConnections,
  scrapeCitizen,
  scrapeClassic,
  scrapeGarage,
  scrapeGlasgowComedyFest,
  scrapeGlasgowDoorsOpen,
  scrapeGlasgowFilmFest,
  scrapeGlasgowGreenEvents,
  scrapeGlasgowInternational,
  scrapeGlasgowJazzFest,
  scrapeGlasgowMela,
  scrapeGlasgowPride,
  scrapeHampdenParkConcerts,
  scrapeHydro,
  scrapeKingTutsWah,
  scrapeMerchantCityFest,
  scrapeMono,
  scrapeNice,
  scrapeO2academy,
  scrapeOran,
  scrapePipingLiveFest,
  scrapePoetry,
  scrapeSec,
  scrapeStereo,
  scrapeSubClub,
  scrapeSwg3,
  scrapeTrnsmtFestival,
  scrapeWestEndFestival,
  scrapeWorldPipeBandChamps,
  scrapeBambooGlasgow,
  scrapeSWG3V2
};
